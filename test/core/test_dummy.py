"""A seed test to make sure that the core (no-extras) ci actually passes."""

# avoid annoying error
import connectfour  # pylint: disable=unused-import


def test_dummy():
    """Replace this with tests of core (not-extras) functionality."""
    assert True
